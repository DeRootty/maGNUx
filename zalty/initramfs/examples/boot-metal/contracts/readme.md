# contracts

Contratos semióticos de arranque para el ejemplo `/boot/metal/`.

Aquí se declararán las condiciones que el initramfs de Zalty debe comprobar antes de entregar el control al sistema operativo.

Contratos previstos:

- `boot.toml`
- `dev.toml`
- `kernel.toml`
- `storage.toml`
